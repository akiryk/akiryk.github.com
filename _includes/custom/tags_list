{% comment %}<!--
This is adapted from JB/tags_list
-->{% endcomment %}

{% if tags_list.first[0] == null %}
  {% for tag in tags_list %} 
    <li class="inline"><a href="{{ BASE_PATH }}{{ site.JB.tags_path }}#{{ tag }}-ref" class="tag">{{ tag }}</a> </li>
  {% endfor %}
{% else %}
  {% for tag in tags_list %} 
    <li class="inline"><a href="{{ BASE_PATH }}{{ site.JB.tags_path }}#{{ tag[0] }}-ref" class="tag">{{ tag[0] }}</a></li>
  {% endfor %}
{% endif %}

{% assign tags_list = nil %}
