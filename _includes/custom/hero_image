{% comment %}<!--
  This is for the large image at top of post pages as well as the home page and other pages.
  Assign a backup image in case one hasn't been set.
  The data-picture class is for use with picture-fill polyfill, which selects the best image
  size according to screen size. 
  The sizes (site.AK.med_size and other) are set in config.yml.
-->{% endcomment %}
{% if page.hero == nil %}
  {% if page.category == "portfolio" %}
    {% assign pic-name = "portfolio" %}
  {% else %}
    {% assign pic-name = "cassis" %}
    {% assign caption = "Hotel sign, Cassis, France" %}
  {% endif %}
{% else %}
  {% assign pic-name = page.hero %}
  {% assign caption = page.hero-caption %}
{% endif %}
  <div id="start" class="pic-hero">
    <span data-picture data-alt="{{ page.hero-caption }}">
      <span data-src="/assets/images/heros/{{ pic-name }}-mini.jpg" ></span>
      <span data-src="/assets/images/heros/{{ pic-name }}-small.jpg" data-media="(min-width: 420px)"></span>
      <span data-src="/assets/images/heros/{{ pic-name }}-med.jpg" data-media="(min-width:690px)"></span>
      <span data-src="/assets/images/heros/{{ pic-name }}-large.jpg" data-media="(min-width: 760px)"></span>
      <noscript>
        <img src="/assets/images/heros/{{ pic-name }}-small.jpg">
      </noscript>
    </span>
    {% if caption %}
      <div class="caption">{{ caption }}</div>
    {% endif %}
  </div>