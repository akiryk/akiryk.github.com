{% unless page.tags == empty %}
  <h6 class="slug">This post tagged:</h6>
  <ul class="post-tags inline">
    {% assign tags_list = page.tags %}
    {% include JB/tags_list %}
  </ul>
{% endunless %}  